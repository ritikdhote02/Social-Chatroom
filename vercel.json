{
    "builds": [{
        "src": "chatappprj/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "chatappprj/wsgi.py"
        }
    ]
}